import java.util.ArrayList;

public class Weapon extends Trait {

	public Weapon(StatMods SM, RollMods RM, int PC, ArrayList<Trait> PR,
			String Name, String Description) {
		super(SM, RM, PC, PR, Name, Description);
		
	}
}
