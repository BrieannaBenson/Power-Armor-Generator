import java.util.ArrayList;

public class Defenses extends Trait {

	public Defenses(StatMods SM, RollMods RM, int PC, ArrayList<Trait> PR,
			String Name, String Description) {
		super(SM, RM, PC, PR, Name, Description);
		
	}
}
