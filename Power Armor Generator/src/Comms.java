import java.util.ArrayList;

public class Comms extends Trait {

	public Comms(StatMods SM, RollMods RM, int PC, ArrayList<Trait> PR,
			String Name, String Description) {
		super(SM, RM, PC, PR, Name, Description);
		
	}
}
