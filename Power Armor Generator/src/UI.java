import java.util.ArrayList;

public class UI extends Trait {

	public UI(StatMods SM, RollMods RM, int PC, ArrayList<Trait> PR,
			String Name, String Description) {
		super(SM, RM, PC, PR, Name, Description);
		
	}
}
