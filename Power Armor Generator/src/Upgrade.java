import java.util.ArrayList;

public class Upgrade extends Trait {

	public Upgrade(StatMods SM, RollMods RM, int PC, ArrayList<Trait> PR,
			String Name, String Description) {
		super(SM, RM, PC, PR, Name, Description);
		
	}
}
